---
description: Valida um caso sintético sem simular
agent: medical-twin
---

Carregue a skill `medical-digital-twin` e execute `medical_twin_validate` usando o caminho informado em `$ARGUMENTS`. Quando nenhum caminho for informado, use `examples/synthetic_case.json`. Informe somente a validade estrutural e os bloqueios encontrados.
