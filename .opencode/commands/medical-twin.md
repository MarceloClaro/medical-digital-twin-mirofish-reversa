---
description: Simula um gêmeo digital médico sintético local
agent: medical-twin
---

Carregue a skill `medical-digital-twin` e execute `medical_twin_simulate`.

Argumentos: `$ARGUMENTS`

Formato: `<case_file> [horizon] [seed] [output_file]`.

Use `examples/synthetic_case.json`, horizonte `4` e seed `42` quando faltarem valores. Apresente um resumo do artefato, da revisão Reversa e dos gates de segurança. Não interprete índices como resultados clínicos.
